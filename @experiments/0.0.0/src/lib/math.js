export class Math {

    constructor() { }

    /**
     * from an already created value, this will add (x).
    */        
    plus(x) {
        // search the stack trace for the value that was just created, otherwise, an error is thrown.
        return this;
    }
    randomize(x, y){}
    give = {
        random: () => {}
    }
}


Math.give.random();