/**
 * License: MIT LICENSE
 * Copyright: github/@hazl-dev
 * Date: December 31, 2025
 * 
 * Description: this is the core-system implementation of meta-language in javascript.
 * this module provides hidden, fundamental parts of the meta-language core system.
 * read more in the system documentation online [here]()
 */
let README;

/**
 * @summary this is the lowest-level thing in meta-language.
 * @description in essence, all blocks begin as __abstract__ by nature. there is no shape, or form at this stage, only pure function. 
 * @abstract this thing describes the concept of something, before it takes shape. this is, philosophically, at the stage where infinite choices become tangible as potential. the potential is then translated into kinetic energy, and thus, life is formed.
 * @description the purpose of the abstract layer, is to provide core essential operations, that all blocks inherit from.
 */
export class Abstract {
    constructor(){

    }

}

/**
 * this is where all blocks are first created
 */
export class Block extends Abstract {
    /**
     * this operation will allow a block to communicate with another block, aslong as they are in the same stack.
     * more complex relationships are coming soon.
     */
    switch(block){

    }    
}

/**
 * all data in meta-language flows visibly. in order for this to be implemnted across the system, transfers and changes to data are visible to the engineer.
 */
export class Channel {

}